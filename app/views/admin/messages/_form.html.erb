<%= form_with model: message, url: url do |f| %>
  <%= render 'layouts/error_messages', resource: message %>
  <%= f.text_area :message, class: "form_field", rows: 2 %>
  <%= f.submit submit, class: "submit-btn" %>
<% end %>
